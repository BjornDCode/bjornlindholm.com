<Narrow title="Select text on contenteditable element">
    <Title>Select text on contenteditable element</Title>
    <Subheadline class="block mt-2">29/03/2020</Subheadline>

    <Prose class="mt-6">
        <Paragraph>
            A small helper function to select all text in a contenteditable element.
        </Paragraph>

        <CodeBlock>const selectElementContents = element => {
const range = document.createRange()
range.selectNodeContents(element)

const selection = window.getSelection()
selection.removeAllRanges()
selection.addRange(range)
}</CodeBlock>
    </Prose>
</Narrow>