<Narrow>
    <Data>
        date: 1970-01-01
    </Data>

    <Title>Links</Title>
    
    <ul class="mt-8">    
        @each(page in $pages.Links.pages.sortBy('date', 'desc'))
            @if(page.name not equals 'Index')
                <li>
                    <ContentLink href="{{ page.path }}">
                        {{ page.title }}
                    </ContentLink>
                </li>
            @endif
        @endeach
    </ul>
</Narrow>